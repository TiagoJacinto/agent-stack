# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` at the repository root if it exists, and inspect relevant decisions under `docs/adr/`.

If these files do not exist, proceed silently. Create them lazily when domain terms or architectural decisions require them.

Use the glossary vocabulary when naming domain concepts. Surface any conflict with an existing ADR instead of silently overriding it.
