# Agent instructions

1. Read `README.md`, `features/create-minimum-project.feature`, and `src/catalog.ts` before changing generation behavior.
2. Treat approved Gherkin scenarios as the observable product contract.
3. Add capabilities through the feature catalog and generated manifest rather than creating independent preset templates.
4. Work on one feature at a time and leave both this repository and generated fixtures passing.
5. Run `bun run check` before declaring work complete.
